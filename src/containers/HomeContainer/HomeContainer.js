import { BackGround, Feature } from "../../components";
import {
    Container,
} from "./styledHomeContainer";

const HomeContainer = () => {
    return (
        <Container>
            <BackGround />
            <Feature />
        </Container>
    );
};

export default HomeContainer;
