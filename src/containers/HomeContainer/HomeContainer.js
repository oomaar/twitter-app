import { BackGround } from "../../components";
import {
    Container,
} from "./styledHomeContainer";

const HomeContainer = () => {
    return (
        <Container>
            <BackGround />
        </Container>
    );
};

export default HomeContainer;
